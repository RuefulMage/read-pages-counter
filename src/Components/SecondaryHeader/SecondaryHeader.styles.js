import {makeStyles} from "@material-ui/styles";

const secondaryHeaderStyles = makeStyles({
  root: {
    background: '#FFFFFF',
    padding: '15px',
    boxShadow: '0px 10px 20px rgba(31, 32, 65, 0.05)',
  }
});

export { secondaryHeaderStyles };