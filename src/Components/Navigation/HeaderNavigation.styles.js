import { makeStyles } from '@material-ui/styles';

const headerNavigationStyles = makeStyles({
  root: {
    marginLeft: '30px',
  },
  list: {
    display: 'flex',
    listStyle: 'none',
    margin: '0',
  },
});

export { headerNavigationStyles };
